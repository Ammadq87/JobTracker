class Notification {
    /**
     * 
     * @param {number} status 
     * @param {string} msg 
     */
    constructor(status, msg) {
        this.status = status
        this.msg = msg
    }
}

module.exports = Notification